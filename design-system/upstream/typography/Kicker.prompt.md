All-caps Michroma kicker label — the small letterspaced line above every heading or panel section.

```jsx
<Kicker>Bridge console · Cool</Kicker>
<Kicker size="lg">Terminal & CLI design system</Kicker>
```

Sizes: `sm` (0.18em), `md` (0.2em, default), `lg` (13px / 0.25em, page-level).
