Config-file code block — filename header bar over a `<pre>`. Deliberately neutral near-black (#0b0b0d) in both themes, matching the source's CONFIG FILES section.

```jsx
<CodeBlock filename="~/.zshrc (excerpt)" code={`eval "$(oh-my-posh init zsh)"`} />
```

Lay out in a 2-column grid with 20px gap.
