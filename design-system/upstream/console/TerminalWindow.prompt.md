Terminal window frame — chrome bar with three theme-tinted dots (steel / secondary / primary — never macOS red-yellow-green), centered title, 8px radius, deep shadow.

```jsx
<TerminalWindow title="zsh — lcars-padd">
  <SegmentBar />
  <div>❯ git status</div>
</TerminalWindow>
```
