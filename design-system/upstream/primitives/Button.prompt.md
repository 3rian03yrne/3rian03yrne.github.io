Console action button — JetBrains Mono, bold, uppercase, 6px radius, dark text on accent fill.

```jsx
<Button onClick={run}>Engage</Button>
<Button variant="ghost" size="sm">Cancel</Button>
```

Hover brightens 10%, press darkens. `disabled` drops to 40% opacity. Note: extrapolated primitive — the source defines no buttons.
